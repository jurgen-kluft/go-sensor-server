
- We actually do not need to specify the mac-address, we could save the incoming data under the mac-address directly.
  Then in the folder we only need to add a .json file that describes the device (name, location, type, ...).

